services:
  todo-app:
    container_name: todo-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8085:8080"
    volumes:
      - ./data:/app/data
